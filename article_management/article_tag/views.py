from django.shortcuts import render

# Create your views here.
# for x in tag_name:
        #     i=0
        #     y=x[i]
        # print(y)
        # tag_list=Tag.objects.values("tag_name").first()
        # demo=tag_list['tag_name'[1]]
        # print(demo)
            

        

        # tag_data=Tag(tag_name=tag_name)
        # tag_data.save()
        # tag=Tag.objects.values("id").order_by('-id').first()
        # tag_id=tag['id']
        # article_data=Article(user_id=user_id,title_name=title,content=content,tag_id=tag_id)
        # article_data.save()